/*function perimetro (radio){
    return 2*Math.PI*radio;
}
function area (radio){
    return Math.PI*radio*radio;
}
console.log(perimetro(3), area(3))
*/
/*let longitudNombreCompleto = (nombre, apellido) => {
nombre + " " + apellido
    return nombreCompleto.length 
}
console.log (longitudNombreCompleto("Tomas", "Garcia"))

function sePoneLaOlla (diaDeLaSemana) {
    if (diaDeLaSemana == "Domingo"){
      return "¡Pongamos la olla, hoy se comen pastas!"
    }else{
        return "Mejor lo dejamos para el domingo"
    }
}

/*
let suma = (num1, num2) => num1 + num2;
let resta = (num1, num2) => num1 - num2;
let multiplicacion = (num1, num2) => num1 * num2;
let division = (num1, num2) => num1 / num2;
let calculadora = (num2, num1, operacion) => operacion(num1,num2);
console.log(calculadora(12,2, suma))
*/

setTimeout(() => console.log("hola bb"),1000)
